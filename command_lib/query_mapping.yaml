query:
  demand: select sum(Quantity) as total_sale_qty, Description, Country from sales_table group by Description, Country
  revenue: select sum(UnitPrice*Quantity) total_revenue, Description, Country from sales_table group by Description, Country